import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    root:{
        alignItems: 'center',
        padding: 20,
        flex: 1,
        width: '100%',
        height: 50,
    },
    lable: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#000000',
    }
});

export default styles;
